const nomes = ['Alana', 'Albert', 'Alexandre', 'Agatha', 'Carlos', 'Daniel']
const cidades = ['Aluminio', 'Mairinque', 'São Roque']
const bairros = ['Jardim vitória']
const notas = [100, 90, -50]



nomes.push ('Vitor') //adicionar nomes

nomes.push ('Licia', 'Leonardo') //adicionar nomes

nomes.sort () //Ordena a lista

nomes.slice (-3)

console.log (nomes)

console.log (nomes [1]) // Ver os nomes na lista de nomes

console.log (nomes.length)  // Ver a quantidade de nomes que possui dentro dessa lista

